# Развертывание BCL Master контракта

## 🎯 Цель

Развернуть BCL Master контракт с кастомными параметрами:
- **fullPriceTon**: 300 TON (вместо 3 TON)
- **feeAddress**: UQDjQOdWTP1bPpGpYExAsCcVLGPN_pzGvdno3aCk565ZnQIz

## 📋 Подготовка

### 1. Установите зависимости

```bash
npm install
```

### 2. Установите переменную окружения с мнемониками

**Windows PowerShell:**
```powershell
$env:MNEMONIC="word1 word2 word3 ... word24"
```

**Windows CMD:**
```cmd
set MNEMONIC=word1 word2 word3 ... word24
```

**Linux/Mac:**
```bash
export MNEMONIC="word1 word2 word3 ... word24"
```

⚠️ **ВАЖНО**: Никогда не коммитьте мнемоники в репозиторий!

### 3. Убедитесь, что на кошельке достаточно TON

Минимум 0.2 TON для развертывания контракта.

## 🚀 Запуск развертывания

```bash
npm run deploy:bcl
```

Или напрямую:

```bash
npx tsx scripts/deploy-bcl-master.ts
```

## 📊 Что делает скрипт

1. ✅ Подключается к TON блокчейну
2. ✅ Загружает ваш кошелек из мнемоников
3. ✅ Получает код jetton wallet из TON SDK
4. ✅ Создает начальные данные с параметрами:
   - `ctx_max_ton` = 300 TON
   - `ctx_fee_address` = ваш адрес для комиссий
5. ✅ Создает StateInit контракта
6. ✅ Вычисляет адрес контракта
7. ✅ Отправляет транзакцию развертывания

## 📝 После развертывания

1. Дождитесь подтверждения транзакции (обычно 5-10 секунд)
2. Проверьте контракт на блокчейне (адрес будет выведен в консоли)
3. Обновите `MASTER_ADDRESS` в `src/config.ts`:

```typescript
export const TONFUN_CONFIG = {
  // ...
  MASTER_ADDRESS: "ваш_новый_адрес_контракта",
  // ...
};
```

4. Перезапустите приложение

## ⚠️ Важные замечания

- Скрипт использует ваш bytecode BCL master контракта
- Код jetton wallet берется автоматически из TON SDK
- Адрес для комиссий уже настроен: `UQDjQOdWTP1bPpGpYExAsCcVLGPN_pzGvdno3aCk565ZnQIz`
- fullPriceTon установлен на 300 TON

## 🔍 Проверка развертывания

После развертывания проверьте контракт на:
- [TONScan](https://tonscan.org/)
- [TONViewer](https://tonviewer.com/)

Вставьте адрес контракта, который будет выведен в консоли.

## ❓ Проблемы?

Если возникли проблемы:

1. **Ошибка "MNEMONIC not set"**
   - Убедитесь, что установили переменную окружения

2. **Ошибка "Insufficient balance"**
   - Пополните кошелек минимум на 0.2 TON

3. **Ошибка подключения к TON API**
   - Проверьте интернет-соединение
   - Попробуйте другой endpoint в скрипте

4. **Ошибка компиляции TypeScript**
   - Убедитесь, что установлены все зависимости: `npm install`
